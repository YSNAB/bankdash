export const metadata = {
  title: 'POS Systeem',
  description: 'Point of Sale systeem',
};

export default function POSLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
